<UserControl
    x:Class="HPADesign.Styles.EditableTextBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HPADesign.Styles"
    xmlns:Convert="using:HPADesign.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <Grid>
        <TextBlock Text="{Binding Mode=TwoWay, Converter={StaticResource b}}" Visibility="Visible" Name="TextBlock" Tapped="TextBlock_Tapped"/>
        <TextBox Text="{Binding Mode=TwoWay, Converter={StaticResource b}}" Visibility="Collapsed" Name="TextBox" LostFocus="TextBox_LostFocus" />
    </Grid>
</UserControl>
