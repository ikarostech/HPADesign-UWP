<UserControl
    x:Class="HPADesign.Views.Tabs.AirfoilView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HPADesign.Views.Tabs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:Custom="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="1200" Height="700">

    <Grid Height="Auto" Width="1200" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Button Height="53" Width="53" Content="Add" VerticalAlignment="Top" Click="{x:Bind airfoilviewmodel.AddAirfoil}" Margin="296,126,0,0"/>

        <Custom:DataGrid Height="200" Width="400" ItemsSource="{x:Bind airfoilviewmodel.AirfoilList}" AutoGenerateColumns="False">
            <Custom:DataGrid.Columns>
                <Custom:DataGridTextColumn Header="名前" Binding="{Binding Name}" />

            </Custom:DataGrid.Columns>
        </Custom:DataGrid>
        <Polygon Stroke="Red" Margin="200,400"  Points="{x:Bind airfoilviewmodel.AirfoilPoints.Value,Mode=TwoWay}"/>

    </Grid>
</UserControl>
